# CPU training with the existing Omniglot embeddings, plus plotting utilities.
# Use Python 3.9-3.12 (the project setup guide uses Python 3.10).
# Install: python -m pip install -r requirement.txt
# Keep the JAX stack on versions compatible with this repository's older APIs.
jax==0.4.26
jaxlib==0.4.26
equinox==0.11.4
optax==0.2.2
chex==0.1.86
jaxtyping==0.2.28

# nptyping uses NumPy aliases removed in NumPy 2.
numpy==1.26.4
scipy==1.12.0
nptyping==2.5.0
h5py==3.11.0
matplotlib==3.8.4
tqdm==4.66.4

# Optional extras (not needed for the basic training run):
# wandb                # --use_wandb experiment tracking
# submitit             # Slurm sweep scripts
# torch, torchvision   # Re-extracting image embeddings
# jupyterlab, ipywidgets # Interactive notebooks
